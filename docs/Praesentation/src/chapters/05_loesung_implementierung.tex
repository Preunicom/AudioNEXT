\section{Lösung / Implementierung}
\begin{frame}{Lösung / Implementierung - TODO - MAX}

\end{frame}
\begin{frame}{Lösung / Implementierung - TODO - FELIX}

\end{frame}
\begin{frame}{Lösung / Implementierung - TODO - RICHI}

\end{frame}
\begin{frame}{Lösung / Implementierung - VIS AXI HW [Lonthoff]}
    \begin{itemize}
        \item AXI4-Lite Slave Interface für Registerzugriff
        \item AXI4-Stream Master Interface für Datenübertragung an VGA IP
    \end{itemize}
\end{frame}
\begin{frame}{Lösung / Implementierung - VIS CORE SW  [Lonthoff]}
   \begin{itemize}
        \item Software-API für die VIS CORE IP
        \item Initialisierung der IP und Konfiguration der Register
        \item Polling der IP-Statusregister (FDP, WD)
        \item Schreiben von Zeichen in den Textframebuffer
        \item Berechnung der Farbe für die Lautstärkebalken
    \end{itemize}
\end{frame}
\begin{frame}{Lösung / Implementierung - VIS CORE SW TB [Lonthoff]}
    \begin{itemize}
        \item Testbench für die VIS CORE Software-API
        \item Testet die Initialisierung, Registerkonfiguration und Zeichenanzeige
        \item Simuliert die Lautstärkebalkenanzeige
    \end{itemize}
\end{frame}
\begin{frame}{Lösung / Implementierung - VGA - Übersicht [Remy]}
    \begin{itemize}
        \item 8x16 Pixelblöcke für ein Zeichen
        \item ROM mit 8x16 Pixelmaps für die ASCII Daten für niedrige Speichernutzung
        \item Nur Verweis auf ASCII Daten im ROM werden im Textframebuffer gespeichert
        \item 12 Bit Farbe pro Zeichen
        \item Schreiben neuer Werte in den Farb- \& Textframebuffer während des nicht sichtbaren Bereichs
    \end{itemize}
\end{frame}
\begin{frame}{Lösung / Implementierung - VGA - Steuerung [Remy]}
    \begin{figure}
        \centering
        \includegraphics[height=0.9\textheight, keepaspectratio]{VGA_Bufferbefuellung}
    \end{figure}
\end{frame}
\begin{frame}{Lösung / Implementierung - VGA - Bildausgabe [Remy]}
    \begin{figure}
        \centering
        \includegraphics[height=0.9\textheight, keepaspectratio]{VGA_Textmodus}
    \end{figure}
\end{frame}

\begin{frame}{Lösung / Implementierung -- State-Chart}
    @ Hafeneder?
    % TODO: State-Chart einer relevanten Zustandsmaschine einfügen,
    %       z.B. VGA-Steuerung oder Audio-Sampling.
    %       Entweder als Grafik (\includegraphics{...}) oder als TikZ-Automat:
    \begin{figure}
        \centering
        \begin{tikzpicture}[->, >=Stealth, node distance=2.8cm, on grid, auto]
            \node[state, initial] (s0) {IDLE};
            \node[state] (s1) [right=of s0] {STATE\,1};
            \node[state] (s2) [right=of s1] {STATE\,2};
            \path
                (s0) edge node {bedingung} (s1)
                (s1) edge node {bedingung} (s2)
                (s2) edge [bend left=45] node {reset} (s0);
        \end{tikzpicture}
        \caption{Platzhalter -- State-Chart (TODO: durch reale FSM ersetzen).}
    \end{figure}
\end{frame}
